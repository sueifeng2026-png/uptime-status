version: "3.8"
services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/prisma
    environment:
      - DATABASE_URL=file:/app/prisma/data.db
    restart: unless-stopped
